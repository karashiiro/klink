// Note: Not using `as const` here to allow these to be assigned to string fields
export const DEFAULT_COLORS: {
  readonly BACKGROUND: string;
  readonly PRIMARY: string;
  readonly SECONDARY: string;
} = {
  BACKGROUND: "#1a1a1a",
  PRIMARY: "#364163",
  SECONDARY: "#a58431",
};
